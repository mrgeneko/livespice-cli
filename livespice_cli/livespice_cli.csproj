<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <AssemblyName>livespice_cli</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <!-- PRISTINE LiveSPICE (extern/LiveSPICE, a pinned submodule) — never patch it in place.
         See README.md: a tool measured against a patched copy of itself proves nothing.
         If you need patched simulator behavior, fork this repo — don't edit the submodule. -->
    <ProjectReference Include="../extern/LiveSPICE/Circuit/Circuit.csproj" />
    <ProjectReference Include="../extern/LiveSPICE/ComputerAlgebra/ComputerAlgebra/ComputerAlgebra.csproj" />
    <ProjectReference Include="../extern/LiveSPICE/Util/Util.csproj" />
  </ItemGroup>
</Project>
